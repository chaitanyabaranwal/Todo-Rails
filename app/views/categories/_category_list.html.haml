- if categories.empty?
  %span.text-warning There are no categories!
- else
  %table.table.table-hover.table-bordered
    %thead
    %tr
      %th Name
      %th Description
      %th
    %tbody
    - categories.each do |category|
      %tr
      %td
        %strong= category.name
      %td.text-info= category.description
      %td
        = link_to category_path(category), remote: true, method: :delete, data: {confirm: "Are you sure you want to delete category '#{category.name}'?"} do
          %i.icon-remove
      %td
        = link_to edit_category_path(category), remote: true do
          %i.icon-edit